# Python 3.13 compatible requirements for RAG system
# Using packages with available wheels for Python 3.13

# Core web framework - minimal FastAPI setup
fastapi
uvicorn
python-dotenv

# Basic utilities
httpx
tqdm
requests

# Scientific computing (should have Python 3.13 wheels)
numpy
pandas

# PDF processing
pdfplumber

# Text processing
nltk

# OpenAI integration (essential for LLM)
openai

# Development tools
pytest
black

# Skip for now (will install manually if needed):
# - sentence-transformers (may need compilation)
# - chromadb (may need compilation) 
# - langchain (has many dependencies)
# - scikit-learn (may need compilation)
# - pydantic v2.x (needs Rust compilation)
